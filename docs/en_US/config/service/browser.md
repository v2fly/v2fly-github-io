# Browser Forwarder
* Name: `browser`
* Type: Service
* ID: `service.browser`

> `listenAddr`: string

The listening address for the browser forwarder.

> `listenPort`: number

The listening port for the browser forwarder.
